{
  "scripts": {
    "docs:dev": "vitepress dev docs --host --port 8081",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host --port 8081"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.16"
  }
}
